# Easier navigation: .., ..., ...., ....., ~ and -
alias ..="cd ../"
alias ...="cd ../../"
alias ....="cd ../../../"
alias .....="cd ../../../../"
alias ~="cd ~"

# or use Navigate by .1 .2 .3 .4 for ../ ../../ ../../../ ../../../../
dotSlash=""
for i in 1 2 3 4
do
    dotSlash=${dotSlash}'../';
    baseName=".${i}"
    alias $baseName="cd ${dotSlash}"
done

# Shortcuts
alias vidir="cd ~/.vim/"
alias d="cd ~/Dropbox"
alias dl="cd ~/Downloads"
alias dt="cd ~/Desktop"
alias bt="cd ~/\•\ BT\ Transfers" 
alias dv="cd ~/_Creative/Development"
alias sch="cd ~/Google\ Drive/School-Fall2014/"
alias SYMB="cd ~/Google\ Drive/School-Fall2014/ITCS\ 3152\ -\ Symbolic\ Programming/"
alias OSNW="cd ~/Google\ Drive/School-Fall2014/ITCS\ 3146\ -\ Operating\ Syst\ and\ Networking/"
alias Cube="cd ~/Google\ Drive/School-Fall2014/ITCS\ 4230\ -\ Intro\ to\ Game\ Design\ \&\ Dvlpmnt/Assignment\ 4/CubeTest/"
alias lo=logout

# Applicaions
alias vim="/Applications/Utilities/MacVim.app/Contents/MacOS/Vim"
alias vi="mvim"
alias edit="mvim"

# Detect which `ls` flavor is in use
if ls --color > /dev/null 2>&1; then # GNU `ls`
    colorflag="--color"
else # OS X `ls`
    colorflag="-G"
fi

# List all files colorized in long format
alias l="ls -lF ${colorflag}"

# List all files colorized in long format, including dot files
alias la="ls -laF ${colorflag}"

# List only directories
alias lsd="ls -lF ${colorflag} | grep --color=never '^d'"

# 
alias timer='echo "Timer started. Stop with Ctrl-D." && date && time cat && date'

# Always use color output for `ls`
alias ls="command ls ${colorflag}"

# Hide/show all files in finder
alias hide="defaults write com.apple.finder AppleShowAllFiles -bool false && killall Finder"
alias show="defaults write com.apple.finder AppleShowAllFiles -bool true && killall Finder"

# Hide/show all desktop icons
alias hidedesktop="defaults write com.apple.finder CreateDesktop -bool false && killall Finder"
alias showdesktop="defaults write com.apple.finder CreateDesktop -bool true && killall Finder"

# lock the screen when going AFK
alias afk="/System/Library/CoreServices/Menu\ Extras/User.menu/Contents/Resources/CGSession -suspend"

# change mkdir to mkdir && cd
function mkdr
{
    command mkdir $1 && cd $1
}
